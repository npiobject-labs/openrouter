# Sesión 2026-09-13 11:08 — Etapas 7 y 8: el plan completo

**Qué cambió.** Etapa 7: `stream: true` sirve los eventos según se generan, con el medidor
en `Drop` para que una respuesta cancelada quede anotada con lo que costó hasta ahí, y
`primer_token_ms` como columna nueva. Etapa 8: alias (`"model": "alias:mapeador"`) con
parámetros por defecto que no pisan lo que manda quien llama, cadena de respaldo que solo
salta cuando el fallo es del proveedor, y simulador de coste que no llama a nadie. El
contrato sube a 0.8.0 con las ocho etapas entregadas; la consola gana respuesta en vivo y
panel de alias (build `OP-B1-20260913-016`).

**SHA.** `e4f9644` en la rama `claude/wizardly-goldberg-oxriz7`, tres commits: etapa 7,
etapa 8 y las notas de despliegue en VPS. **El pull request no se pudo abrir**: la API de
GitHub devolvió `500` seis veces seguidas a `POST /repos/.../pulls`, con cuerpo vacío, en
cuatro redacciones distintas y también sin borrador. La rama está empujada y el PR se abre
en cuanto GitHub responda.

**URL para probar.** <https://npiobject-labs.github.io/openrouter/consola.html> y
<https://npiobject-labs.github.io/openrouter/api.html> (ambas después de fusionar a `main`;
Pages solo publica desde la rama por defecto). Backend:
<https://openrouter-npiobject-labs.fly.dev/salud>.

**Drive.** No se subió copia: esta sesión solo tiene permisos de lectura sobre Drive
(`search`, `read`, `download`), no de escritura. Queda pendiente de subir a mano o desde
una sesión con el conector completo.

**Qué falta.** Rotar `SERVICIO_CLAVE`, que sigue expuesta desde las sesiones del día 12.
Abrir el PR y fusionar. Y el despliegue en VPS, anotado en `despliegue-vps.md`: faltan los
secretos `VPS_HOST` y `VPS_SSH_KEY`, decidir si el VPS sustituye a Fly o convive con él
(la base SQLite no se puede compartir entre dos máquinas) y el terminador TLS para
`apisor.oracle402.com`.
