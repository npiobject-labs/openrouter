# Sesión 2026-09-12 11:20 — Plan por etapas y etapa 1 en producción

- **Qué cambió**: `docs/planificacion/plan-etapas.md` con las ocho etapas del servicio; `docs/index.html` pasa a ser el mock de todas las etapas (el mock 0 se archiva en `docs/mocks/001-mock-0.html`); se elimina `.github/workflows/vigilancia-fly.yml`; y se entrega la **etapa 1**: el backend se parte en módulos y publica `GET /v1/estado` y `POST /v1/chat/completions`, con clave de servicio obligatoria en todo `/v1`, CORS como capa del router y `docs/consola.html` como cliente de referencia. `deploy.yml` vuelca los secretos del servicio y verifica la conexión con OpenRouter.
- **SHA**: `dd5e27e` en `main`, vía squash del pull request [#5](https://github.com/npiobject-labs/openrouter/pull/5); antes `27a83ad` (plan y mock, pull request [#4](https://github.com/npiobject-labs/openrouter/pull/4)).
- **URL para probar**: https://npiobject-labs.github.io/openrouter/consola.html con la clave de servicio; el mock de las ocho etapas sigue en https://npiobject-labs.github.io/openrouter/ (build `OP-B1-20260912-005`). El run [34685500997](https://github.com/npiobject-labs/openrouter/actions/runs/34685500997) dejó el despliegue en `success` y su paso de verificación registró `HTTP 200` en `/v1/estado`: el servicio habla con OpenRouter. Comprobado además desde el navegador, con la etiqueta de la clave, el límite de 5 $ y el modelo por defecto en pantalla.
- **Resultado en Drive**: subido `plan-etapas.md` a la carpeta del proyecto y este resumen a `sesiones/`.
- **Qué falta**: la consulta a un modelo no se ha probado todavía desde la consola, solo la conexión. Etapa 2 (catálogo de modelos) sin empezar. Sigue pendiente de sesiones anteriores decidir qué hacer con la carpeta antigua de Drive y con `08 Openrouter`.
